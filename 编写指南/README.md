# SPR 27 电控培训教材 · 编写指南

> 我们教什么？不是函数，是**机制**；不是代码，是**思维**。
> 表层的函数和代码只是表象——每一个知识点，都要能回答"背后发生了什么"。
> 这份指南统一"怎么写"，让每个人写出来的章节，像出自同一双手，更出自同一颗心。

---

## 〇、这本教材的灵魂（动笔前必读）

### 0.1 表象之下是机制

新生看到的，是一个函数。你要让他看到的，是函数背后的**一整条链**。

> **标准示范** —— 写一个"置高电平"函数，背后发生了什么？

表象（一个调用）：
```c
HAL_GPIO_WritePin(GPIOB, GPIO_PIN_0, GPIO_PIN_SET);   // 让 PB0 输出高电平
```

**往下挖到"能解释机制"就够了**（这是达标线，不是天花板）：
1. **API 层**：这个函数不是魔法，它最终只做一件事——往一个"和引脚对应的寄存器"里写一个值。
2. **寄存器 / 硬件层**：芯片里每个引脚都对应一组寄存器，写特定寄存器 = 控制这个引脚输出高低。这些寄存器的地址、每一位什么意思，都写在芯片手册（Reference Manual）里——所以"怎么控制引脚"不是猜的，是**查手册查出来的**。CPU 把值写进寄存器 → 内部电路传到引脚 → 引脚变高 → LED 亮。


**示范的分寸感（重要）：**
- 深度不够 = 打发（只给函数不给机制，语法糖）❌
- 深度太深 = 劝退（一上来就晶体管物理 / 寄存器竞态）❌
- **合适的深度 = 让新生"哦，原来是这样"**：讲清"函数背后是寄存器、是手册"这一层就达标，再往深了留给想深入的人当彩蛋 ✅

### 0.2 我们培养的不是"仅仅会调库的人"更是调库之外的东西

每一章都该指向这四种能力：
1. **独立思考** —— 遇到没见过的模块，知道怎么拆开它，而不是等答案。
2. **自己诊断问题** —— bug 不可怕，可怕的是不会找。法医式排查是贯穿所有板块的主线。
3. **形成自己的思维体系** —— 学完不是记住一堆散点，而是拼出一张"系统怎么工作"的图。
4. **善于利用工具** —— AI、手册、调试器、示波器都是手的延伸；会用工具的人，走得远。

### 0.3 态度：前辈的严谨、慎重与热爱

- **严谨**：你写下的每个字新生会当真。技术细节自己先验证，别把错的教给别人。
- **慎重**：拿不准就标"存疑"并去查证，不猜、不糊弄。
- **热爱**：写自己真正觉得酷的东西。热爱会传染，凑合不会。

### 0.4 尊重新生的时间 = 尊重自己的时间

- 一句话能讲清的，绝不用一段话；一段话的，绝不用一页。
- 每一段都要有**信息量**，灌水是对读者最大的不尊重。
- 排版清晰、路径明确。读者省下的时间，就是你的时间。

---

## 一、两大贯穿主线（每个板块都在埋线，不是"本板块的事"）

### 1.1 模块化思维

教函数，更要教"为什么要有函数"：
- **接口与实现分离**：调用方只依赖接口、不依赖实现——所以"置高电平"可以封装成 `setPinHigh(pin)`，换芯片不改业务代码。
- **信号流图**：每个模块画"输入 → 处理 → 输出 → 反馈"。排查问题 = 在信号流上打桩（隔离变量）。
- **分层**：驱动层 / 中间层 / 业务层。让新生从小习惯"代码是分层的，不是一坨"。

### 1.2 通讯运行机制

教"调函数"，更要教"数据怎么从 A 到 B"：
- 表象：`HAL_UART_Transmit(&huart1, data, len, 1000);`
- 机制：数据进发送数据寄存器 → 移位寄存器 → 按波特率一位位从 TX 出去（起始位→数据位→校验位→停止位）→ 对方 RX → 移位寄存器 → 接收数据寄存器 → 读出来。
- 往下挖：波特率从哪来？（时钟分频 USARTDIV）为什么要起始位 / 停止位？（帧同步）为什么两边波特率必须一致？（位时间对齐）
- 05 板块是主战场，但 02 的 printf、04 的传感器、06 的控制回传——处处都在用通讯，处处都埋线。

---

## 二、教学法：四步走，每一步都"向下挖一层"

| 步骤 | 要做什么 | 关键 |
|---|---|---|
| ① 问题抛出 | 抛真实场景 / 疑问，让他觉得"跟我有关" | 别用"今天我们学 GPIO"这种标题 |
| ② 引导推演 | 不给答案，给"挖"的工具："如果换你来做，会怎么想？" | 苏格拉底式，引导代替灌输 |
| ③ 机制讲解 | 按 0.1 的分寸：API → 寄存器 / 硬件，挖到"能解释"即可 | **全篇核心，不许跳过** |
| ④ 验收 | 可动手交付件 + "能解释" | 见 2.1 验收哲学 |

> 反例：20 条操作步骤平铺，新生看完就忘。❌
> 正例：先问"怎么让 LED 亮？"→ 引导到引脚 / 寄存器 → 三层机制 → 验收"改到另一个引脚，并解释为什么地址变了"。✅

### 2.1 验收哲学（所有板块统一）

- 交付件不是"跑通"，是"**跑通 + 能讲清为什么这么写、别的方式为什么不行**"。
- 抄来的代码跑通，不算学会。
- 每章末尾有"交付件 / 验收"表格（参考 01 板块 README）。

---

## 三、写作红线（踩了就不合格）

1. **语法糖禁令**：只给 API 不给机制 = 不合格。`为什么` 永远优先于 `怎么做`。
2. **名词不解释 = 不合格**：首次出现必须有一句话总结。
3. **不能是纯步骤流水账**：每一步都要有"为什么"。
4. **先体感后理论**：先让 LED 亮、电机转，再上规范。规范是痛苦的解药，不是额外负担。
5. **横切主线必须埋线**：诊断能力（printf 埋线 → 标定 → 心跳 → 08 总结）、Git（从 02 贯穿）——本板块涉及就主动呼应。
6. **AI 内容必须验证**：AI 生成的代码 / 结论，去官方 Reference Manual 交叉验证，并注明"已验证"。
7. **不教"怎么办"**：教提问三段式——"发生了什么 / 我做了什么 / 我猜是什么原因"。
---

## 四、如何教"思考与诊断"

### 4.1 法医式排查（能力主线）
- **先稳定复现，再动手改**：偶发 bug 复现不了 = 没有证据。
- **二分法**：砍掉一半可能性，缩小真凶范围。
- **信号流打桩**：不知道系统有哪些环节就没法排查——排查前先画信号流图。

### 4.2 提问三段式
所有答疑、所有文档里的"问与答"，都按"发生了什么 / 我做了什么 / 我猜是什么原因"引导。没有排查记录的提问，电控组不受理。

### 4.3 工具怎么教
- **查手册**：教快速定位法（框图 → 寄存器表 / 功能说明 → 时序图），不是从头读。
- **用 AI**：教"如果…那么…"式的提问、教交叉验证、教它当"第二大脑"而不是"作业代写机"。
- **调试器 / 示波器 / 逻辑分析仪**：从 02 起就让工具成为手的延伸。

---

## 五、分工：一人一个板块

| 板块 | 主题 | 负责人 | 状态 |
|---|---|---|---|
| 00 | 总纲与入队契约 | 队长 / 项管 | ✅ |
| 01 | 认知篇：重新定义"电控" | 待认领 | 🔶 1.1 / 1.2 完成 |
| 02 | 第一台能"动"的机器 | 待认领 | 🔶 进行中 |
| 03 | 工程素养：公司化流程 | 待认领 | ⬜ |
| 04 | STM32 核心外设 | 待认领 | ⬜ |
| 05 | 通信与协议 | 待认领 | ⬜ |
| 06 | 控制算法 | 待认领 | ⬜ |
| 07 | 代码架构 | 待认领 | ⬜ |
| 08 | 法医式诊断 | 待认领 | ⬜ |
| 09 | 项目协作与赛季管理 | 待认领 | ⬜ |
| 10 | 附录与资源库 | 待认领 | ⬜ |

- 填名 push 即认领；一次认领 1 个板块，验收通过再认领下一个（防"挖坑不填"）。
- 每个板块配一个"影子"（防单点故障），写完后能用 10 分钟把"为什么这么设计"讲给影子听。

---

## 六、Git / GitHub 协作（VSCode 版）

仓库：`https://github.com/darkcome6/27-SPR.git`（默认分支 `main`）

### 6.1 先说人话：分支 / 主干是干什么的

**main（主干 / 主分支）** = 项目的"正式版"。这是大家公认的那一份，谁都能看，但谁都不能在上面随手乱改。想象它是已出版的教材：可以修订，但不能随便涂鸦。

**分支（branch）** = 从主干上"叉"出去的一条工作线，像树枝。
- 机制上它不是复制品，只是一个"指向某次提交的标签"——所以建分支几乎不占空间。
- 你在自己的分支上随便改，**不影响主干，也不影响别人**。
- 改完，把分支"接回"主干 = 合并（merge / PR），Git 会做差异比对，把你改的部分合进去。

**为什么一个人时可以推 main，多人时必须用分支？**
- 一个人：没人和你抢，推 main 永远安全。
- 多人：都直接推 main，两人同时改同一个文件 → 后推的人要么覆盖别人、要么报冲突，乱成一锅粥。
- 分支 + PR = 给每个人一块独立的"工作间"，互不打扰，最后统一"验收合并"。

**PR（Pull Request）到底是什么？**
- 你推完分支，在 GitHub 上发起"申请把我的分支合并进 main"。
- 它是一个**验收关口**：别人能看到你改了什么、能评论、确认没问题才合并。
- 对咱们团队：就是"写完让负责人看一眼，再进正式版"。

**首次**：装 Git → VSCode 登录 GitHub → `Ctrl+Shift+P` → `Git: Clone` → 粘贴地址 → 配置身份：
```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

**日常**：
```bash
git pull                            # 1. 先拉最新，别在旧版上改
git checkout -b 02-toolchain        # 2. 建自己的板块分支
git add 02-第一台能"动"的机器/
git commit -m "docs(02): 完成工具链配置讲义"
git push -u origin 02-toolchain     # 3. 推送
```
→ GitHub 上 New pull request 合并到 `main` → `git checkout main && git pull`

- 提交规范：`docs(02): 描述`、`fix(03): 描述`；写清"改了什么、为什么"，别写 `update`、`111`。
- 冲突：VSCode 里处理 `<<<<<<< ======= >>>>>>>` 标记，保留两边都对的部分。
- 红线：别推大二进制（docx / xlsx 用文本模板）；别 `push -f`；提交前看 `git status`。

---

## 七、写完自查清单（机制导向）

- [ ] 每个知识点都能回答"背后发生了什么"？有没有只给 API 不给机制的地方？
- [ ] 挖到"能解释机制"了吗（API → 寄存器 / 硬件）？深度对新生合适吗（别太深也别太浅）？
- [ ] 模块化 / 通讯机制两条主线，本板块埋线了吗？
- [ ] 四步法齐全（问题 → 推演 → 机制 → 验收）？
- [ ] 交付件"能跑 + 能解释"？
- [ ] 有没有灌水？每一段都有信息量吗？（尊重新生的时间）
- [ ] 术语首次出现有"一句话总结"？
- [ ] 技术细节自己验证过了吗？（严谨）
- [ ] commit 规范？PR 已合并？
---

> 最后记住：**我们要的不是一堆文档，是一批能独立干活、能自己想办法的人。**
> 文档是手段，人才是目的。写的时候问自己一句——
> **新生读到这，是学到了一个函数，还是看懂了这个世界是怎么转的？**
